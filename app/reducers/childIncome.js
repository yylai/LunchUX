
const initial = {
    
}

export default function childIncome(state = initial, action) {
    
    switch (action.type) {
        case 'ADD_CHILD_INCOME':
        
        
        default:
            return state;
    }
}